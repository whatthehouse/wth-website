---
import '../styles/global.css';
import settings from '../data/settings.json';
export interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  noindex?: boolean;
}
const {
  title,
  description = 'WHAT THE HOUSE บริการตรวจบ้านและตรวจคอนโดก่อนรับมอบในเชียงใหม่ ลำพูน ลำปาง ตรวจละเอียดทุกระบบ รายงาน PDF ไทย-อังกฤษ | Home Inspection Chiang Mai',
  ogImage = '/assets/img/logo.jpg',
  noindex = false,
} = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---
<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="keywords" content="ตรวจบ้านเชียงใหม่, ตรวจคอนโดเชียงใหม่, ตรวจบ้านก่อนโอน, ตรวจรับบ้าน, Home Inspection Chiang Mai, Condo Inspection Chiang Mai" />
  {noindex && <meta name="robots" content="noindex, nofollow" />}
  <link rel="canonical" href={canonicalURL} />

  <!-- Open Graph -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonicalURL} />
  <meta property="og:image" content={ogImage} />

  <!-- Favicon -->
  <link rel="icon" href="/assets/img/logo.jpg" type="image/jpeg" />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

  <!-- Google Analytics 4 (set settings.gaId to enable) -->
  {settings.gaId && (
    <>
      <script async src={`https://www.googletagmanager.com/gtag/js?id=${settings.gaId}`}></script>
      <script is:inline define:vars={{ gaId: settings.gaId }}>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', gaId);
      </script>
    </>
  )}

  <!-- Schema: LocalBusiness (also read as the site's Organization) -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "@id": "https://whatthehouse.net/#business",
    "name": "WHAT THE HOUSE – Home Inspections",
    "description": "บริการตรวจบ้านและตรวจคอนโดก่อนรับมอบในเชียงใหม่ ลำพูน ลำปาง ตรวจละเอียดทุกระบบ รายงาน PDF ไทย-อังกฤษ",
    "url": "https://whatthehouse.net",
    "telephone": settings.phone,
    "email": settings.email,
    "image": "https://whatthehouse.net/assets/img/logo.jpg",
    "logo": "https://whatthehouse.net/assets/img/logo.jpg",
    "priceRange": "฿฿",
    "currenciesAccepted": "THB",
    "paymentAccepted": "Cash, Bank transfer, PromptPay",
    "knowsLanguage": ["th","en"],
    "areaServed": [
      { "@type": "City", "name": "Chiang Mai" },
      { "@type": "City", "name": "Lamphun" },
      { "@type": "City", "name": "Lampang" }
    ],
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Chiang Mai",
      "addressRegion": "Chiang Mai",
      "addressCountry": "TH"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": 18.8037866,
      "longitude": 99.0182146
    },
    "openingHoursSpecification": [{
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"],
      "opens": "08:00",
      "closes": "20:00"
    }],
    "sameAs": [settings.facebookUrl, settings.lineUrl],
    "slogan": "BETTER HOMES • BETTER LIVING",
    "hasOfferCatalog": {
      "@type": "OfferCatalog",
      "name": "Home inspection & construction services",
      "itemListElement": [
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Home & Condo Handover Inspection / ตรวจรับบ้านและคอนโด" } },
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Home Construction Supervision / ควบคุมงานสร้างบ้าน" } },
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Pre-Construction Planning / วางแผนก่อนสร้างบ้าน" } },
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Renovation & Extension Advisory / ที่ปรึกษางานต่อเติม" } },
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Home Problem Diagnosis / ตรวจหาสาเหตุปัญหาบ้าน" } },
        { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Contractor Work Quality Check / ตรวจคุณภาพงานผู้รับเหมา" } }
      ]
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "telephone": settings.phone,
      "email": settings.email,
      "contactType": "customer service",
      "areaServed": "TH",
      "availableLanguage": ["Thai","English"]
    }
  })} />
</head>
<body>
  <slot />

  <!-- Netlify Identity (handles invite/recovery links -> redirects to CMS login) -->
  <script src="https://identity.netlify.com/v1/netlify-identity-widget.js" is:inline></script>
  <script is:inline>
    (function () {
      if (window.netlifyIdentity) {
        window.netlifyIdentity.on("init", function (user) {
          if (!user) {
            window.netlifyIdentity.on("login", function () {
              document.location.href = "/admin/";
            });
          }
        });
      }
    })();
  </script>

  <!-- Language + mobile nav script -->
  <script>
    (function () {
      "use strict";
      var KEY = "wth_lang";
      function applyLang(lang) {
        document.body.classList.toggle("en", lang === "en");
        document.documentElement.lang = lang === "en" ? "en" : "th";
        document.querySelectorAll("[data-lang-btn]").forEach(function (b) {
          b.classList.toggle("on", b.getAttribute("data-lang-btn") === lang);
        });
        try { localStorage.setItem(KEY, lang); } catch (e) {}
      }
      var saved = "th";
      try { saved = localStorage.getItem(KEY) || "th"; } catch (e) {}
      applyLang(saved);
      document.addEventListener("click", function (e) {
        var btn = (e.target as HTMLElement).closest("[data-lang-btn]");
        if (btn) applyLang(btn.getAttribute("data-lang-btn") || "th");
      });

      // mobile nav
      document.addEventListener("DOMContentLoaded", function () {
        var burger = document.querySelector(".burger");
        var links = document.querySelector(".nav-links");
        if (burger && links) {
          burger.addEventListener("click", function () {
            links!.classList.toggle("open");
          });
          links.addEventListener("click", function (e) {
            if ((e.target as HTMLElement).closest("a")) links!.classList.remove("open");
          });
        }

        // FAQ accordion
        document.querySelectorAll(".faq-q").forEach(function (q) {
          q.addEventListener("click", function () {
            (q.parentElement as HTMLElement).classList.toggle("open");
          });
        });
      });
    })();
  </script>
</body>
</html>
